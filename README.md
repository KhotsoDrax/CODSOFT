# CODSOFT 🚀
Internship Programme

## Numbers Game 🎲

The Numbers Game is a simple Java console application where the user tries to guess a randomly generated number between 1 and 100. The game offers four difficulty levels:

- `Easy` 😃
- `Medium` 😐
- `Hard` 😨
- `Very Hard` 🥵

Each level has varying numbers of attempts allowed.

### How to Run

1. Ensure you have Java installed on your machine.
2. Download the `NumberGame.java` file.
3. Open a terminal or command prompt.
4. Navigate to the directory containing `NumberGame.java`.
5. Compile the program using:
   ```
   javac NumberGame.java
   ```
6. Run the program using:
   ```
   java NumberGame
   ```
7. Follow the on-screen instructions to play the game.

## Student Grade Calculator 📚

The Student Grade Calculator is a Java program that calculates the average grade of a student based on input grades for different subjects. It prompts the user to enter the number of subjects and corresponding grades, then calculates and displays the average grade.

### How to Run

1. Ensure you have Java installed on your machine.
2. Download the `GradeCalculator.java` file.
3. Open a terminal or command prompt.
4. Navigate to the directory containing `GradeCalculator.java`.
5. Compile the program using:
   ```
   javac GradeCalculator.java
   ```
6. Run the program using:
   ```
   java GradeCalculator
   ```
7. Follow the on-screen instructions to input grades and calculate the average.

## Currency Converter 💱

The Currency Converter is a Java program that allows the user to convert an amount in South African Rand (ZAR) to various other currencies. The user can choose the currency and enter the amount in ZAR, and the program provides the converted amount in the selected currency.

### How to Run

1. Ensure you have Java installed on your machine.
2. Download the `CurrencyConverter.java` file.
3. Open a terminal or command prompt.
4. Navigate to the directory containing `CurrencyConverter.java`.
5. Compile the program using:
   ```
   javac CurrencyConverter.java
   ```
6. Run the program using:
   ```
   java CurrencyConverter
   ```
7. Follow the on-screen instructions to input the amount in ZAR and choose the currency for conversion.

## Compiling and Running from Terminal 💻

For all programs, follow these general steps:

1. Open your terminal or command prompt.
2. Navigate to the directory containing the Java file using the `cd` command. For example:
   ```
   cd path/to/your/java/files
   ```
3. Compile the Java file using the `javac` command followed by the filename. For example:
   ```
   javac NumberGame.java
   ```
4. Run the compiled program using the `java` command followed by the class name (without the .java extension). For example:
   ```
   java NumberGame
   ```

> **Note:** Make sure to keep your Java environment up-to-date for the best performance! 💻✨

> **Tip:** If you encounter any "command not found" errors, ensure that Java is correctly installed and that your system's PATH includes the Java bin directory.